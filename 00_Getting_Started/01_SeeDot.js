function setup() {
	describe('Make a point with fat stroke weight.');
	createCanvas(400, 400);

	background(255);    // Clear the Screen White
	stroke(255, 0, 0);  // Set the Color
	strokeWeight(18); 	// Set the Width
	point(200, 200); 	// Make your Point!
}
